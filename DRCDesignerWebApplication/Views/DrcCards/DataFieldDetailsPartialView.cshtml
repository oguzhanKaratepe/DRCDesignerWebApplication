@using DRCDesignerWebApplication.ViewModels
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class=""><h5 class="align-content-center"><%- data.AttributeName %> Details</h5></div>

<div class="border rounded p-2">
@(Html.DevExtreme().Form<FieldViewModel>()
      .ElementAttr("class", "address-form form-container")
      // .FormData(new JS("tabExtras"))
      
      .ColCount(2)
      .Items(items => {
         
                           items.AddSimpleFor(m => m.MaxValue);
                            items.AddSimpleFor(m => m.MinLength);
                            items.AddSimpleFor(m => m.Required);
                        
      }
      ))
    
</div>